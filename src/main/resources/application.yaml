spring:
  datasource:
    url: jdbc:h2:mem:segurosbolivarbd;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: 'admin'
    password: 'S3gur0s'

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    show-sql: true

  h2:
    console:
      enabled: true